import { User, Test, Patate } from './Controllers/user';
import { RoutesToSDK } from '../Lib';

const test = new Test();
export const routes = {
  [`user`]: new User(),
  assx: {
    tsb: new User(),
    ro: { t: test },
  },
  n: {
    t: new Patate(),
  },
  straight: new Patate(),
};
